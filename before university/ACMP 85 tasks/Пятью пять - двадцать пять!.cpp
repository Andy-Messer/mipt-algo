#include <iostream> 
  
using namespace std;
  
int main() {
    long a, sum;
    cin >> a;
    if (a != 5) {
        sum = a / 10;
        a = sum * (sum + 1); cout << a << 25;
    }
    else {
        cout << 25;
    }
     
    return 0;
  
}